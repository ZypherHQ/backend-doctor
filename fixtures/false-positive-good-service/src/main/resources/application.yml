management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
spring:
  jpa:
    open-in-view: false
  datasource:
    password: ${EXAMPLE_DB_PASSWORD}
