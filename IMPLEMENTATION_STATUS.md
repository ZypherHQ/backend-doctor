# Implementation Status

Last updated: 2026-05-20

## Phase 0 Bootstrap

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| Rust workspace | Rust worker | Verified | `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` passed in Phase 1 verification. |
| CLI crate and `backend-doctor --help` | Rust worker | Verified | `backend-doctor-cli` was exercised through fixture scans, `--json`, `--score`, and `--verbose` in Phase 1 verification. |
| Node npm wrapper skeleton | Phase 0 packaging/docs/status worker | Complete | `npm/backend-doctor/package.json`, `bin/backend-doctor.js`, `README.md`, and wrapper smoke test exist. |
| Fixtures directory | Rust worker | Present | `fixtures/empty/.gitkeep` exists. |
| Schemas directory | Rust worker | Present | `schemas/backend-doctor-config.schema.json` and `schemas/backend-doctor-report.schema.json` exist. |
| CI workflow scaffold | Phase 0 packaging/docs/status worker | Complete | `.github/workflows/ci.yml` exists and gates Rust, npm wrapper, fixture, and schema checks. |
| Root README quick start | Phase 0 packaging/docs/status worker | Complete | `README.md` exists. |
| Documentation scaffold | Phase 0 packaging/docs/status worker | Complete | `docs/**` contains CLI, config, scoring, rules, CI, privacy, release, and agent docs. |
| Codex skill scaffold | Phase 0 packaging/docs/status worker | Complete | `skills/backend-doctor/SKILL.md` exists. |

## Phase 1 Finding Model, Config, Scoring, Report Output

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| Rule and finding models | Phase 1 Rust implementation worker | Verified | `cargo test --workspace` passed with 20 tests. |
| Config parsing and defaults | Phase 1 Rust implementation worker | Verified | `cargo test --workspace` passed with 20 tests. |
| Scoring | Phase 1 Rust implementation worker | Verified | `cargo run -p backend-doctor-cli -- fixtures/empty --score` printed exactly `100`. |
| Terminal and verbose report output | Phase 1 Rust implementation worker | Verified | `cargo run -p backend-doctor-cli -- fixtures/empty --verbose` produced useful verbose output. |
| JSON report output | Phase 1 Rust implementation worker | Verified | `cargo run -p backend-doctor-cli -- fixtures/empty --json` produced parseable JSON with score `100` and no findings. |
| Empty fixture acceptance | Phase 1 Rust implementation worker | Verified | `cargo run -p backend-doctor-cli -- fixtures/empty --json` completed successfully with score `100` and no findings. |
| Formatting, linting, and tests | Phase 1 verifier | Verified | `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` passed. |

## Phase 2 Stack Detection And Project Graph

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| Repository inventory and root detection | Phase 2 implementation/re-verifier | Verified | Phase 2 re-verification passed `cargo run -p backend-doctor-cli -- fixtures/polyglot --debug` and JSON output with detected project graph data. |
| Language detection with confidence | Phase 2 implementation/re-verifier | Verified | `cargo run -p backend-doctor-cli -- fixtures/polyglot --json` reported Go, Java, and Node language detection with score `100`. |
| Monorepo service graph | Phase 2 implementation/re-verifier | Verified | Polyglot JSON verification reported three services. |
| Framework and infrastructure detection | Phase 2 implementation/re-verifier | Verified | Polyglot verification reported Express, Gin, Spring Boot, OpenAPI `api/openapi.yaml`, and migration `db/migrations/001_init.sql`; debug output included `openApiSpecs=1` and `migrationFiles=1`. |
| Diff plumbing and debug/JSON exposure | Phase 2 implementation/re-verifier | Verified | Phase 2 re-verification passed polyglot `--debug`, `--json`, and `--score`; debug output had no secret-like values. |
| Empty fixture compatibility | Phase 2 implementation/re-verifier | Verified | Empty fixture JSON path still completed successfully with score `100`. |
| Formatting, linting, and tests | Phase 2 re-verifier | Verified | `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` passed in Phase 2 re-verification. |

## Phase 3 Go Plugin MVP

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| Go heuristic rule scanner | Phase 3 verifier | Verified | `cargo run -p backend-doctor-cli -- fixtures/go-bad-service --verbose --json-out /tmp/go.json` produced score `0`, `29` findings, and `1` suppressed finding. |
| Go JSON output parity | Phase 3 verifier | Verified | Direct `cargo run -p backend-doctor-cli -- fixtures/go-bad-service --json` matched the verbose plus `--json-out` scan behavior. |
| Go safe-fix dry-run | Phase 3 verifier | Verified | `cargo run -p backend-doctor-cli -- fixtures/go-bad-service --fix-safe --dry-run` reported two `go/gofmt-required` safe fixes without modifying files. |
| Go rule metadata and evidence | Phase 3 verifier | Verified | Sampled findings included location, evidence, remediation, and fix metadata; secret-like evidence checks passed. |
| Regression coverage | Phase 3 verifier | Verified | Empty and polyglot regression scans still passed after Phase 3. |
| Formatting, linting, and tests | Phase 3 verifier | Verified | `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` passed. |

Phase 3 release scope is verified. Go support combines fixture-backed backend rules with deterministic local execution. Commit `e7e3b8a5ce98017a44955d3b800cbc4612fcb899` records `go/sql-string-concat` consuming Go AnalysisFacts for local request-source-to-SQL sink flows, with post-commit fmt, focused Go SQL/rules/analysis tests, full rules/workspace tests, clippy, `git diff --check`, Docker build, and Docker help smoke passing. Commit `e058a0e` records Go HTTP timeout AnalysisFacts evidence: the Go adapter emits `SinkKind::NetworkRequest` with `adapter=go` for `http.Get`, `http.Post`, and `http.DefaultClient.Do` before generic SQL matching, while preserving `SqlQuery` for `db.Query`, `db.Exec`, and `tx.Query`; `go/http-client-no-timeout` consumes current-file Go NetworkRequest facts with usable ranges and those target sink names, de-dupes same-line helper fallback, and preserves `http.Client{}` and SQL behavior. Network-backed or credentialed external tool execution is opt-in.

## Phase 4 Node/TypeScript Plugin MVP

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| Node/TypeScript heuristic rule scanner | Phase 4 re-verifier | Verified | `cargo run -p backend-doctor-cli -- fixtures/node-express-bad-service --verbose --json-out /tmp/node.json` produced score `15`, grade `Critical`, and `26` findings. |
| Node JSON output and verbose evidence | Phase 4 re-verifier | Verified | The same verbose plus `--json-out` scan produced location, evidence, remediation, and guided fix metadata; `node/floating-promise` was present at `src/routes/auth.ts:7:3`. |
| Node safe-fix dry-run | Phase 4 re-verifier | Verified | Dry-run reported only two safe `node/console-log-production` fixes and did not modify the fixture. |
| Node SQL AnalysisFacts consumer | Node SQL AnalysisFacts worker/tester/reviewer | Verified locally | `node/sql-string-concat` now consumes real Node/TypeScript AnalysisFacts for request-source-to-SQL sink flows. The Node adapter prunes broad same-scope request-to-SQL edges and emits Node-specific edges only when SQL argument contribution evidence exists; it tracks simple alias overwrites, skips nested callable bodies, and suppresses unsanitized edges for parameterized query values and query-object aliases. Rule consumption ignores wrong, unusable, unrelated, and command-sink facts; preserves heuristic fallback; dedupes same-line reports; and keeps parameterization boundaries explicit. |
| Node command AnalysisFacts consumer | Node command AnalysisFacts worker/tester/reviewer | Verified locally | Commit `f048a78` (`feat: consume node command analysis facts`) records `node/child-process-shell-injection` consuming Node AnalysisFacts for local request-source-to-command sink flows. The Node adapter emits sync child-process command sinks; the rule preserves fallback and same-line de-dupe; shell semantics remain conservative. Context7 remained quota-blocked, so official Node `child_process` docs were used as fallback: https://nodejs.org/api/child_process.html. |
| Node eval AnalysisFacts consumer | Node eval AnalysisFacts worker/tester/reviewer | Verified locally | Commit `c27a64c` (`feat: consume node eval call facts`) records `node/eval-or-function-constructor` consuming Node `CallFact` entries for direct `eval(...)` calls from structured current-file facts. Fact-backed eval emits from usable facts; same-line text fallback de-dupes; usable Node facts suppress comment/string false positives for `eval(` when no real eval call exists; `new Function(...)` remains text fallback until constructor/new-expression facts exist; absent, wrong-file, wrong-adapter, stale, unusable, or invalid-range facts preserve fallback. Context7 remained quota-blocked; official ECMAScript docs fallback was used only as general semantic context. |
| Redaction and regression coverage | Phase 4 re-verifier | Verified | Redaction checks passed; regression scans showed Go score `0` with `29` findings, polyglot score `93` with `4` findings, and empty score `100` with `0` findings. |
| Formatting, linting, and tests | Phase 4 re-verifier | Verified | `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` passed. |

Phase 4 release scope is verified. Node/TypeScript support includes fixture-backed backend rules, safe console-log remediation, a local verified `node/sql-string-concat` AnalysisFacts slice for request-source-to-SQL sink flows, local verified `node/child-process-shell-injection` AnalysisFacts evidence for request-source-to-command sink flows, and local verified direct eval `CallFact` evidence for `node/eval-or-function-constructor`. Network-backed audit behavior, all-language SQL parity, all-language command-sink parity, constructor/new-expression eval facts, public release readiness, and broader graph expansion remain explicit opt-ins, external blockers, or roadmap work.

## Phase 5 Java/Spring Plugin MVP

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| Java/Spring heuristic rule scanner | Phase 5 re-verifier | Verified | `cargo run -p backend-doctor-cli -- fixtures/java-spring-bad-service --verbose --json-out /tmp/java.json` produced score `19`, grade `Critical`, and `25` findings. |
| Java/Spring detection and metadata | Phase 5 re-verifier | Verified | The Java/Spring fixture scan detected Java and Spring and included representative Java rule IDs with location, evidence, remediation, and rule metadata. |
| Redaction and targeted locations | Phase 5 re-verifier | Verified | Redaction checks passed for `java/sql-string-concat` and hardcoded secret findings; `java/transactional-remote-call` appeared only at `UserService.java:22`; `java/controller-bypasses-service-layer` appeared only at `UserController.java:27` and `UserController.java:37`. |
| Java SQL AnalysisFacts consumer | Java SQL AnalysisFacts worker/tester/reviewer | Verified locally | Commit `00d37cdd6d3a44fab2847b5ff6c996e4718e109b` records `java/sql-string-concat` consuming Java AnalysisFacts for local request-source-to-SQL sink flows. Review found no actionable correctness, regression, over-reporting, or over-suppression issues. Verification passed clean `git status`, focused Java SQL/rules/analysis tests, full rules/workspace tests, clippy, `git diff --check`, Docker build, and Docker help smoke. |
| Regression coverage | Phase 5 re-verifier | Verified | Go, Node, polyglot, and empty regression scans still passed after Phase 5. |
| Formatting, linting, and tests | Phase 5 re-verifier | Verified | `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` passed. |

Phase 5 release scope is verified. Java/Spring support includes fixture-backed backend rules, Spring detection, DTO-shape fingerprints, redacted evidence, a local verified `java/sql-string-concat` AnalysisFacts slice for request-source-to-SQL sink flows, and local verified WebClient timeout NetworkRequest facts consumed by `java/http-client-no-timeout`. Command-sink fact consumption, all-language SQL parity, all-language HTTP-timeout parity, and broader graph expansion remain roadmap work. External Java build/security tools require explicit configuration.

## Phase 6 Security, Dependencies, And Secrets

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| Security/dependency/secrets heuristic scanner | Phase 6 verifier | Verified | `cargo run -p backend-doctor-cli -- fixtures/security-bad-service --verbose --json-out /tmp/security.json` produced score `0`, grade `Critical`, and `22` findings. |
| JSON output and score caps | Phase 6 verifier | Verified | The same scan wrote `/tmp/security.json`; score caps were verified at `39` and `49` for critical security/dependency cases. |
| Fallback secret scanning and redaction | Phase 6 verifier | Verified | Direct JSON showed `7` `security/hardcoded-secret` findings and `7` secret fingerprints; raw fake secrets were not present in output. |
| Security and supply-chain rules | Phase 6 verifier | Verified | Direct JSON showed `10` security findings, `11` dependency findings, and representative rules including `security/sensitive-logging`, `security/curl-pipe-shell`, `supply-chain/vulnerable-dependency`, unpinned GitHub action, unpinned Docker base, wildcard-version, git-dependency, and missing-lockfile checks. |
| External scanner parser helpers | Phase 6 verifier | Verified | Gitleaks, OSV, Trivy, Semgrep, and CodeQL SARIF parser helpers are covered by tests. This is parser-only coverage. |
| Formatting, linting, and tests | Phase 6 verifier | Verified | `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` passed. |
| Regression fixture scores | Phase 6 verifier | Verified | Regression scans reported Go `0` with `29` findings, Node `11` with `28` findings, Java `0` with `27` findings and cap `39`, polyglot `88` with `7` findings, and empty `100` with `0` findings. |

Phase 6 remains local-first by default. Backend Doctor does not execute Gitleaks, OSV, Trivy, Semgrep, CodeQL, package managers, or network-backed vulnerability tools unless configured. External stdout/stderr capture is bounded, redacted, replay-safe, and timeout cleanup is bounded. SARIF schema validation is covered by the final release gates.

## Phase 7 Infrastructure Plugin

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| Infrastructure heuristic scanner | Phase 7 verifier | Verified | `cargo run -p backend-doctor-cli -- fixtures/infra-bad-config --verbose --json-out /tmp/infra.json` produced score `0`, grade `Critical`, and `88` findings. |
| JSON output parity | Phase 7 verifier | Verified | Direct `cargo run -p backend-doctor-cli -- fixtures/infra-bad-config --json` also produced `88` findings. |
| Infrastructure rule coverage | Phase 7 verifier | Verified | Representative Docker, Compose, Kubernetes/Helm, Terraform, CI, OpenAPI, migration, and cross-layer fallback rules were present. |
| Finding metadata and redaction | Phase 7 verifier | Verified | Sampled findings included location, evidence, remediation, and fix metadata; redaction was verified with `24` redacted findings and no raw `bd_fixture` values. |
| External tool parser helpers | Phase 7 verifier | Verified | Hadolint, Trivy, KubeLinter, Checkov, and Conftest parser helpers are covered by tests. This is parser-only coverage. |
| Formatting, linting, and tests | Phase 7 verifier | Verified | `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` passed. |
| Regression fixture scores | Phase 7 verifier | Verified | Regression scans reported security `0` with `28` findings, Go `0` with `29` findings, Node `11` with `28` findings, Java `0` with `27` findings, polyglot `45` with `21` findings, and empty `100` with `0` findings. |

Phase 7 remains local-first by default. Backend Doctor does not execute Hadolint, Trivy, KubeLinter, Checkov, Conftest, package managers, clusters, or network-backed services unless configured. SARIF schema validation and fixture-backed infra/API/OpenAPI checks are covered by the final release gates.

## Phase 8 Autofix And Remediation

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| Autofix CLI options | Phase 8 docs/status reconciliation | Verified | `--plan-fixes`, `--fix-safe`, `--fix-rule`, `--fix-finding`, `--dry-run`, and `--yes` behavior were independently verified. |
| Safe-fix planning | Phase 8 verifier | Verified | `cargo run -p backend-doctor-cli -- fixtures/infra-bad-config --plan-fixes` produced a remediation plan without applying fixes. |
| Safe-fix dry-runs | Phase 8 verifier | Verified | Go and Node safe-fix dry-runs were non-mutating. |
| Safe-fix application | Phase 8 verifier | Verified | A temporary-copy Node run with `--fix-safe --yes` applied the expected `.backend-doctor.toml` and `console.log` removals. |
| Targeted fix filters | Phase 8 verifier | Verified | `--fix-rule` and `--fix-finding` filters selected the expected fix targets. |
| Guided preview output | Phase 8 verifier | Verified | Guided semantic fixes produced preview output only; they are not applied automatically. |
| Rollback behavior | Phase 8 verifier | Verified | A private `cfg(test)` failure injection hook in the fix crate covers atomic write failure rollback through `atomic_write_failure_restores_original_and_cleans_sidecars`; `cargo test -p backend-doctor-fix` passed 9 tests. |
| Formatting, linting, and tests | Phase 8 verifier | Verified | `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` passed. |

Phase 8 implements safe remediation workflows for supported deterministic fixes and preview-only guided semantic fixes. Full guided semantic auto-apply and broader mutation workflows remain outside the current release policy.

## Phase 9 CI, SARIF, Release

| Surface | Owner | Status | Evidence |
| --- | --- | --- | --- |
| SARIF rendering and readiness | Phase 9 implementation/review | Verified | SARIF output includes `runAutomationDetails` IDs and passed structural/readiness plus SARIF schema gates. |
| GitHub annotations | Phase 9 implementation/review | Verified | Annotation generation is implemented and covered by CI/readiness checks. GitHub-hosted code scanning upload was not executed locally. |
| CI thresholds | Phase 9 implementation/review | Verified | `--fail-on` is implemented for deterministic CI failure behavior. |
| Release dry-run workflow | Phase 9 implementation/review | Verified | Release dry-run workflow, local release binary/checksums/help, npm wrapper tests, and npm package dry-run validation under `npm/backend-doctor` passed. Public publishing and binary distribution require credentials and hosted artifact URLs. |
| CLI MVP commands | Final targeted re-review | Verified | `explain`, `init`, and `install --agent codex --yes` are deterministic MVP commands; reserved advanced flags fail explicitly before subcommands. |
| Agent Slop MVP | Phase 9 implementation/review | Verified | `agent/*` observable-pattern rules, Slop Index JSON/verbose output, and no-authorship-claim language are implemented. |
| Plugin maturity and unsupported languages | Phase 9 implementation/review | First integrations verified | Tier 1 release-scope coverage is implemented with fixture/test coverage. Tier 2 and Tier 3 have shallow fixture-backed first integrations plus unsupported-language detection and `core/unsupported-language-coverage`; full production-grade all-language maturity remains roadmap work. Recent local-only evidence commits add C#/PHP persisted JSON/SARIF goldens (`0e9d1ee`), Kotlin/Scala persisted JSON/SARIF goldens (`875d740`), C/C++ persisted JSON/SARIF goldens (`47f6f1a`), Tier 2 clean persisted JSON/SARIF goldens (`256e109`), Tier 3 clean ruby/kotlin/scala/elixir/c/cpp persisted JSON/SARIF goldens (`a18be1c`), Ruby Rails-shaped Tier 3 bad-service fixture/golden evidence for `ruby/sql-string-interpolation` (`32a49ba`), Ruby SQL interpolation AnalysisFacts consumption (`2604a36`), Kotlin Ktor Tier 3 bad-service fixture/golden evidence for `kotlin/unsafe-sql-string-template` (`01be8c8`), Python route request-validation AnalysisFacts consumption (`a80bdb65`), Go Gin route-auth AnalysisFacts consumption (`8c05162`), Java Spring route-auth AnalysisFacts consumption (`d19dfc6`), Go scanner context refactoring with broad local verification (`55e44a8`), PHP unserialize AnalysisFacts consumption (`e72a86b`), Rust reqwest timeout AnalysisFacts consumption (`6ec90c1`), Python Requests timeout AnalysisFacts consumption (`804d89e`), C# HttpClient timeout AnalysisFacts consumption (`d137055`), Node SQL AnalysisFacts consumption (`ea73ebc`), Node command AnalysisFacts consumption (`f048a78`), Node eval AnalysisFacts consumption (`c27a64c`), Python SQL AnalysisFacts consumption (`0a0530a`), Python subprocess command AnalysisFacts consumption (`c863dd9`), Python YAML call AnalysisFacts consumption (`50766a2`), Go SQL AnalysisFacts consumption (`e7e3b8a`), Go HTTP timeout AnalysisFacts consumption (`e058a0e`), Java SQL AnalysisFacts consumption (`00d37cd`), PHP command-sink AnalysisFacts consumption (`a0bbbea`), and PHP SQL AnalysisFacts consumption (`8ec7fda`). Follow-up `5b6ecf5` records the fix-plan summary assertion update from `safe=2 guided=87 patch=2` to `safe=2 guided=84 patch=2` after generic hardcoded-secret environment-variable reference false positives dropped while uppercase literal secret detection stayed covered. Independent review at HEAD `9bdedc70e0dce24d065172102abee73a38678639` confirmed the five named Tier C repairs are implementation/test covered with no actionable gaps and no source, fixture, or golden update needed: cpp-httplib client calls do not become server routes; Kotlin Spring method mappings do not receive a missing class prefix; Kotlin Ktor context/import gating is covered; Phoenix scoped resources controller qualification is covered; Rails namespaced resources target qualification is covered. The Rails-shaped fixture slice at `32a49ba` adds `fixtures/ruby-rails-bad-service/`, persisted JSON/SARIF goldens, and schema/golden/rule/perf gate coverage; independent review found no high/medium blockers. The Kotlin Ktor fixture slice at `01be8c8` adds `fixtures/kotlin-ktor-bad-service/`, persisted JSON/SARIF goldens, schema validation, Tier 3 language-specific finding validation, and bounded performance sanity; independent review found no high/medium blockers for realism, determinism, or wiring. Targeted verification passed `cargo test -p backend-doctor-analysis cpp_httplib -- --nocapture` 7 tests, `spring_rest_controller_method_route_without_class_prefix` 1 test, `ktor` 3 tests, `extracts_phoenix_router_routes_pipelines_and_live_routes` 1 test, `phoenix_resource` 1 test, `namespaced_rails_resource` 1 test, full `cargo test -p backend-doctor-analysis -- --nocapture` 92 tests, `cargo fmt --all --check`, and `git diff --check`. Rails fixture verification passed Tier 2/3 JSON and SARIF golden snapshot gates, fixture JSON/SARIF schema gates, Tier 3 bounded performance sanity, Tier 3 language-specific finding checks, full locked workspace tests with 118 analysis tests and 247 rules tests, fmt, clippy, and `git diff --check`; Context7 Rails lookup was quota-blocked and official Rails guides were used as fallback. Kotlin Ktor fixture verification at `01be8c8` passed focused Tier 2/3 JSON and SARIF golden snapshot gates, fixture JSON/SARIF schema gates, Tier 3 bounded performance sanity, Tier 3 language-specific finding checks, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, full locked workspace tests with 481 passed / 0 failed, and `git diff --check`. Final verification at `5b6ecf5` passed focused generic-secret and fix-plan summary tests, Tier 2/Tier 3 report schema gates, full `report_schema_gate` with 14 tests, `cargo fmt --all --check`, full locked workspace tests with 418 passed / 0 failed, clippy, `git diff --check`, Docker build, and Docker help smoke. Python YAML evidence at `50766a2` is local call/import fact consumption only and keeps existing textual `SafeLoader` recognition as residual risk. Go HTTP timeout evidence at `e058a0e` is local NetworkRequest fact emission/consumer evidence only and does not claim all-language HTTP-timeout parity. This proves only local shallow fixture-backed Tier 3 clean golden, one Ruby Rails-shaped Tier 3 fixture/golden/perf improvement, one Ruby SQL interpolation AnalysisFacts consumer slice, one Kotlin Ktor Tier 3 fixture/golden/perf improvement, and named adapter/test surfaces; full all-language parity, Kotlin production maturity, Ruby production maturity, Tier 3 parity, complete AnalysisFacts SQL correctness, command-sink parity, constructor/new-expression eval fact support, full graph/parity completion, P0 completion, public release readiness, broad fixture/golden/performance coverage, and final validator readiness remain unclaimed. |
| QA gates | Plan 13 QA implementation/review | Verified | Representative generated JSON reports validate against schema, and a 10,000-finding report-rendering performance smoke gate runs in CI/tests. |
| Deep cache hardening | Final release hardening | Verified | Root `.backend-doctor/cache`, `.backend-doctor/report.json`, `.backend-doctor/report.sarif`, and `.backend-doctor/reports` are excluded from cache hashing and cross-layer port collection while nested project `.backend-doctor` files remain visible. Cache miss/hit comparisons passed for security, infra, Go, and Node fixtures. |
| Redaction hardening | Final release hardening | Verified | Authorization redaction covers Bearer, Basic, Digest, and token variants, including short credentials. |
| Distribution dry runs | Final release hardening | Verified | Docker build/run and Homebrew formula generation passed. Ruby and Homebrew were unavailable locally, so formula syntax/audit must run on macOS/Ruby/Homebrew. |

Tier C route repair evidence was refreshed at commit `1184b6b` (`fix: support rails module scope resources`). The five explicit Tier C repairs now have targeted tests and independent audit/verification evidence: cpp-httplib client calls are not emitted as server routes; Kotlin Spring method mappings without a class prefix are covered; Kotlin Ktor context/import gating is covered; Phoenix scoped resource controller qualification is covered; and Rails namespaced resource target qualification is covered. The same audit found and fixed Rails `scope module: "admin" do resources :articles end` target qualification without adding a path prefix, covered by `qualifies_rails_scope_module_resource_targets_without_path_prefix`. Combined local verification passed the focused analysis tests, `cargo test -p backend-doctor-analysis rails -- --nocapture` with 4 passed, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked` including backend-doctor-analysis 119/119 and backend-doctor-rules 258/258, and `git diff --check`. This is bounded local route/framework evidence only; it does not claim complete route extraction, all-language parity, P0 completion, or public release readiness.

## P0 Local Readiness And Public Release Status

| Area | Current status | Latest evidence | Release-owner note |
| --- | --- | --- | --- |
| Local release and scanner evidence | Strong local evidence; not public readiness | Latest 2026-05-17 broad local verification records current HEAD `dd1ab4f960cc32fbb4496e9f7ed21eacec63d743` (`docs: record suppression hardening evidence`) from initially clean and finally clean `git status --short --untracked-files=all`. `git show --stat --oneline --no-renames HEAD` reported 8 files changed, 40 insertions, and 8 deletions. `cargo fmt --all --check`, `cargo check --workspace --all-targets --locked` (2.05s), `cargo test --workspace --all-targets --locked`, and `cargo clippy --workspace --all-targets --locked -- -D warnings` (0.12s) passed. Workspace tests passed with 338 passed and 0 failed across workspace unit/integration targets. `cargo test -p backend-doctor-cli --test report_schema_gate -- --nocapture` passed with 13 tests in 0.88s. `scripts/release/test-local-release-metadata.sh` passed and generated local artifact manifest, provenance draft, and SPDX draft under `/tmp/backend-doctor-local-release-metadata.ALWp8F/metadata` as local-only metadata evidence, not publication evidence. Docker evidence passed with `docker --version` reporting Docker version 29.2.1, build a5c7197, `docker build -t backend-doctor:local-verification-current .`, image manifest list `sha256:ef62e9f42c4e4dea5edcaa4d0c9012bc427687519c3f32aa28066e4fb6cc3de5`, and `docker run --rm backend-doctor:local-verification-current --help` printing CLI usage and commands `explain`, `rules`, `init`, `install`, and `help`. `/usr/bin/npm` exists, but root `package.json` is absent, so npm repo checks were skipped. `brew` and `ruby` were unavailable on PATH. `git diff --check` passed. This remains local-only evidence, not public registry digest/provenance/SBOM evidence. | Treat this as PR-ready local evidence only, not public availability. |
| Representative output modes | Passed locally | Latest post-patch verification confirms `CLICOLOR_FORCE=1` forces ANSI in human output with remediation text and score `44 / 100 Critical`; `NO_COLOR` overrides force; `CLICOLOR=0`, `--ci`, and `--json` contain no ANSI, and JSON parses successfully. Earlier `fixtures/polyglot-monorepo` representative evidence recorded score `44 / 100 Critical`, JSON with 23 findings, and SARIF 2.1.0 with 23 results. Infra JSON/SARIF artifacts parse with no ANSI; SARIF is 2.1.0 with 88 results. Fix-plan dry run prints `Fix plan (dry run)`, `safe=2 guided=87 patch=2`, and remediation previews, with review confirming no JSON/score/SARIF summary leakage. | Clean public runtime output evidence is still missing until public install paths exist. |
| Node SQL AnalysisFacts consumer | Passed locally; not public readiness | Commit `ea73ebcba49a9fc0bac260bcacad961ffad0c7bb` records the `node/sql-string-concat` AnalysisFacts slice. It consumes real Node/TypeScript AnalysisFacts for request-source-to-SQL sink flows with conservative producer/consumer gates: SQL argument contribution evidence is required, broad same-scope request-to-SQL edges are pruned, simple alias overwrites are tracked, nested callable bodies are skipped, parameterized query values and query-object aliases suppress unsanitized edges, wrong/unusable facts are ignored, command-sink facts are ignored, heuristic fallback is preserved, and same-line duplicates are avoided. Final reviewer found no high/medium blockers. Focused verification passed `cargo test -p backend-doctor-analysis request_to_sql_edges -- --nocapture` with 5 tests, `cargo test -p backend-doctor-rules node_sql_ -- --nocapture` with 7 tests, `cargo test -p backend-doctor-rules node_ -- --nocapture` with 23 tests, and `cargo test -p backend-doctor-analysis node -- --nocapture` with 11 tests. Final tester passed `cargo fmt --all --check`, the focused Node SQL/Node analysis/rules suites, `cargo test -p backend-doctor-rules sql_ -- --nocapture` with 11 tests, `cargo test --workspace --all-targets --locked` including backend-doctor-analysis 92, empty_fixture 44, report_schema_gate 13, and backend-doctor-rules 144, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:node-sql-analysisfacts-nested-scope-verification .`, and `docker run --rm backend-doctor:node-sql-analysisfacts-nested-scope-verification --help`. | Local scanner evidence only. This does not claim command-sink coverage, all-language SQL parity, full route graph, full interprocedural taint, Tier A graph readiness, public release readiness, Homebrew/Ruby validation, SECURITY.md owner completion, or P0 completion. |
| Node command AnalysisFacts consumer | Passed locally; not public readiness | Commit `f048a78` (`feat: consume node command analysis facts`) records `node/child-process-shell-injection` consuming Node AnalysisFacts for local request-source-to-command sink flows. The Node adapter emits sync child-process command sinks, and the rule preserves fallback/de-dupe with conservative shell semantics. Final reviewer found no issues. Post-commit tester confirmed `git rev-parse --short HEAD` as `f048a78`; `cargo test -p backend-doctor-analysis node -- --nocapture` 12 passed; `cargo test -p backend-doctor-rules node_child_process -- --nocapture` 8 passed; `cargo test -p backend-doctor-rules node_ -- --nocapture` 31 passed; `cargo fmt --all --check`; full `backend-doctor-rules` 192 passed; `cargo test --workspace --locked` passed, including backend-doctor-analysis 93, backend-doctor-rules 192, CLI integration suites, and doc-tests; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `git diff --check`; `docker build -t backend-doctor:dry-run .`; and `docker run --rm backend-doctor:dry-run --help`. Context7 remained quota-blocked; official Node `child_process` docs were the fallback source: https://nodejs.org/api/child_process.html. | Local scanner and Docker dry-run evidence only. Public release remains blocked: no `origin` remote or local tags, no public repo evidence, npm auth `E401`, no Cargo/Docker/Homebrew/Ruby readiness, expected public prereq failure remains 18 blockers and 2 warnings, and SECURITY.md owner inputs/contacts remain incomplete. |
| Node eval AnalysisFacts consumer | Passed locally; not public readiness | Commit `c27a64c` (`feat: consume node eval call facts`) records `node/eval-or-function-constructor` consuming Node `CallFact` entries for direct `eval(...)` calls from structured current-file facts. Fact-backed eval emits from usable facts; same-line text fallback de-dupes; usable Node facts suppress comment/string false positives for `eval(` when no real eval call exists; `new Function(...)` remains text fallback until constructor/new-expression facts exist; absent, wrong-file, wrong-adapter, stale, unusable, or invalid-range facts preserve fallback. Independent reviewer found no blockers and marked the slice commit-ready. Post-commit verification on `c27a64c` passed clean git status before/after, `cargo test -p backend-doctor-rules node_eval -- --nocapture` 5 tests, `cargo test -p backend-doctor-rules node_ -- --nocapture` 36 tests, `cargo test -p backend-doctor-rules -- --nocapture` 226 tests, `cargo test --workspace --locked` including analysis 97, rules 226, CLI integration 44, and `report_schema_gate` 14, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:dry-run .`, and `docker run --rm backend-doctor:dry-run --help`. Context7 remained quota-blocked; official ECMAScript docs fallback was used only as general semantic context. | Local scanner and Docker dry-run evidence only. This does not claim constructor/new-expression fact support, full graph/parity completion, public release readiness, Homebrew/Ruby/macOS validation, SECURITY.md completion, public validator pass, or P0 completion. |
| Python SQL AnalysisFacts consumer | Passed locally; not public readiness | Committed Python SQL slice at `0a0530a7cfbf47892cc5bc98fe700b92e701c916` modifies `crates/backend-doctor-rules/src/lib.rs`. `python/sql-string-format` consumes Python AnalysisFacts for local request-source-to-SQL sink flows with current-file, request/source/sink/edge/sanitizer, and range gates. It requires direct unsafe SQL construction evidence; preserves heuristic fallback and same-line dedupe; ignores wrong file/language/adapter, missing ranges, command sinks, sanitized edges, parameterized facts, unrelated same-function request reads, and generic `request` names that only substring-match `request_id`. Reviewer found no high/medium blockers after the request-name fix. Verification passed `cargo fmt --all --check`, `cargo test -p backend-doctor-analysis python_ -- --nocapture` with 1 passed, `cargo test -p backend-doctor-rules python_sql -- --nocapture` with 6 passed, `cargo test -p backend-doctor-rules sql_heuristics_still_flag_without_empty_and_unrelated_facts -- --nocapture` with 1 passed, `cargo test -p backend-doctor-rules synthetic_parameterization_facts_do_not_suppress_sql_heuristics -- --nocapture` with 1 passed, `cargo test -p backend-doctor-rules -- --nocapture` with 149 passed, `cargo test --workspace --all-targets --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:python-sql-analysisfacts-request-name-verification .`, and `docker run --rm backend-doctor:python-sql-analysisfacts-request-name-verification --help`. | Local Python SQL evidence only. This does not claim public release readiness, Homebrew/Ruby/macOS validation, SECURITY.md completion, public validator pass, command-sink coverage, all-language route-to-SQL parity, a full route graph, Tier A graph readiness, or P0 completion. |
| Python subprocess command AnalysisFacts consumer | Passed locally; not public readiness | Commit `c863dd9` (`feat: consume python subprocess analysis facts`) records `python/subprocess-shell-true` consuming Python AnalysisFacts for local request-source-to-command sink flows. Detection is gated on top-level `shell=True`, suppresses the real PythonAdapter sanitizer shape (`source->sink` plus `source->sanitizer`), preserves fallback de-dupe, and final reviewer found no defects. Post-commit verification passed `cargo test -p backend-doctor-rules python_subprocess -- --nocapture` 8 tests, `cargo test -p backend-doctor-rules python_ -- --nocapture` 37 tests, `cargo fmt --all --check`, full `backend-doctor-rules` 200 tests, `cargo test --workspace --locked` including backend-doctor-rules 200 passed, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:dry-run .`, and `docker run --rm backend-doctor:dry-run --help`. Context7 remained quota-blocked; official Python subprocess docs were the fallback source: https://docs.python.org/3/library/subprocess.html. | Local scanner and Docker dry-run evidence only. Public release remains blocked by missing origin/public repo/tags, npm/Cargo/Docker/Homebrew credentials and evidence, SECURITY.md owner inputs, P0 release evidence placeholders, hosted install/runtime/SARIF proof, and public validator pass. |
| Python YAML AnalysisFacts consumer | Passed locally; not public readiness | Commit `50766a2` (`feat: consume python yaml call facts`) records `python/unsafe-yaml-load` consuming Python `CallFact` and `ImportFact` context. Fact-backed detection covers `yaml.load`, `import yaml as y; y.load`, `from yaml import load; load`, and `from yaml import load as yaml_load; yaml_load`; `safe_load` and `SafeLoader` stay quiet; comments and strings stay quiet when usable facts are present; same-line reports de-dupe; and fallback remains for absent, stale, wrong-file, wrong-language, wrong-adapter, missing-hash, invalid-range, wrong-call-adapter, and cross-file YAML facts. Independent final review found no blockers and marked the slice commit-ready, with residual risk limited to existing textual `SafeLoader` recognition. Post-commit verification on `50766a2` passed clean git status before/after, `cargo test -p backend-doctor-rules python_yaml -- --nocapture` 9 tests, `cargo test -p backend-doctor-rules python_ -- --nocapture` 46 tests, `cargo test -p backend-doctor-rules -- --nocapture` 235 tests, `cargo test --workspace --locked` including analysis 97, rules 235, CLI integration 44, and `report_schema_gate` 14, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:dry-run .`, and `docker run --rm backend-doctor:dry-run --help`. Context7 quota remained blocked; official PyYAML docs fallback says `safe_load` and `SafeLoader` are safe-oriented alternatives and bare `yaml.load` is unsafe for untrusted data. | Local scanner and Docker dry-run evidence only. This does not claim public release readiness, Homebrew/Ruby/macOS validation, SECURITY.md completion, public validator pass, all-language parity, full graph/parity completion, or P0 completion. Public P0 remains blocked by missing public repo/tag/hosted release/assets/registry/Homebrew/SARIF/security owner evidence. |
| Go SQL AnalysisFacts consumer | Passed locally; not public readiness | Commit `e7e3b8a5ce98017a44955d3b800cbc4612fcb899` records `go/sql-string-concat` consuming Go AnalysisFacts for local request-source-to-SQL sink flows. Final independent review found no actionable over-reporting, over-suppression, or regression issues across consumer logic, source matching, SQL argument extraction, and real-adapter regressions. Verification passed `cargo fmt --all --check`, `cargo test -p backend-doctor-rules go_sql -- --nocapture` with 14 passed, `cargo test -p backend-doctor-analysis go_ -- --nocapture` with 4 passed, `cargo test -p backend-doctor-rules -- --nocapture` with 163 passed, `cargo test --workspace --all-targets --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:go-sql-analysisfacts-postcommit .` with manifest `sha256:f7a4922a2c49e00886f2fa24818dca51b16b6e07e702e384a26beaee0045df2b` and manifest list `sha256:248844d523ac9bddb9e6f1e731e9931b5f6daaf291ee00ef9d8d303b73da92c7`, and `docker run --rm backend-doctor:go-sql-analysisfacts-postcommit --help`. Non-blocking follow-up: context-variant SQL argument indexes are implemented in the consumer, but the Go adapter currently appears to emit only non-context sink names, so there is no real-adapter `QueryContext`/`ExecContext`/`PrepareContext` regression yet. | Local Go SQL evidence only. This does not claim public release readiness, Homebrew/Ruby/macOS validation, SECURITY.md completion, public validator pass, command-sink coverage, all-language route-to-SQL parity, a full route graph, Tier A graph readiness, or P0 completion. |
| Go HTTP timeout AnalysisFacts consumer | Passed locally; not public readiness | Commit `e058a0e` (`feat: consume go http timeout facts`) records Go NetworkRequest fact emission and `go/http-client-no-timeout` fact consumption. The adapter emits `SinkKind::NetworkRequest` with `adapter=go` for `http.Get`, `http.Post`, and `http.DefaultClient.Do` before generic SQL matching, while preserving `SqlQuery` for `db.Query`, `db.Exec`, and `tx.Query`. The rule consumes current-file Go NetworkRequest facts with `adapter=go`, usable ranges, and target sink names `http.Get`, `http.Post`, and `http.DefaultClient.Do`; it de-dupes same-line helper fallback and preserves `http.Client{}` and SQL rule behavior. Independent end-to-end review found no high/medium blockers. Verification passed `cargo test -p backend-doctor-analysis go -- --nocapture` with 9 passed, `cargo test -p backend-doctor-rules go_http -- --nocapture` with 6 passed, `cargo test -p backend-doctor-rules go_ -- --nocapture` with 31 passed, `cargo test -p backend-doctor-rules -- --nocapture` with 247 passed, `cargo test --workspace --locked` including 118 analysis tests and 247 rules tests, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, and `git diff --check`. Context7 Go docs lookup was attempted and quota-blocked; official Go `net/http` docs were used as fallback. | Local Go HTTP-timeout evidence only. This does not claim public release readiness, Homebrew/Ruby/macOS validation, SECURITY.md completion, public validator pass, all-language HTTP-timeout parity, all-language AnalysisFacts parity, a full route graph, Tier A graph readiness, or P0 completion. |
| Java SQL AnalysisFacts consumer | Passed locally; not public readiness | Commit `00d37cdd6d3a44fab2847b5ff6c996e4718e109b` records `java/sql-string-concat` consuming Java AnalysisFacts for local request-source-to-SQL sink flows. Final review found no actionable correctness, regression, over-reporting, or over-suppression issues; `String.valueOf` TypeCheck facts do not suppress unsafe no-space Java SQL concat; numeric/boolean TypeCheck suppression is constrained to explicit conversion evidence; unrelated same-method sanitizers remain non-suppressing; parameterized SQL stays quiet under low-confidence JavaAdapter facts when the SQL argument is static/non-concatenated; and fact-backed no-space concat, same-line, cross-line, and invalid-fact fallback behavior are covered. Verification passed clean `git status`, `cargo fmt --all --check`, `cargo test -p backend-doctor-rules java_sql -- --nocapture` with 7 passed, `cargo test -p backend-doctor-analysis java_ -- --nocapture` with 1 passed, `cargo test -p backend-doctor-rules -- --nocapture` with 170 passed, `cargo test --workspace --all-targets --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:java-sql-analysisfacts-postcommit .` with manifest `sha256:22dd90939eacbaa309cc4fd47b37f410a54a093358fb6c4f6f8a8d7e5260cf5a` and manifest list `sha256:0b98708f7083c9779b2abc464d9c5b76e53b80addc6460f7968c81cf9dffdb9d`, and `docker run --rm backend-doctor:java-sql-analysisfacts-postcommit --help`. | Local Java SQL evidence only. This does not claim public release readiness, Homebrew/Ruby/macOS validation, SECURITY.md completion, public validator pass, command-sink coverage, all-language route-to-SQL parity, a full route graph, Tier A graph readiness, or P0 completion. |
| Java WebClient timeout AnalysisFacts consumer | Passed locally; not public readiness | Commits `074ceda` (`feat: add java webclient timeout facts`) and `eeafa27` (`feat: consume java webclient timeout facts`) record Java WebClient `SinkKind::NetworkRequest` fact emission and `java/http-client-no-timeout` fact consumption. The adapter emits safe WebClient facts with `adapter=java` plus optional `timeoutEvidence=present` and `timeoutEvidenceKind` metadata; coverage includes `retrieve`, `exchange`, `exchangeToMono`, `exchangeToFlux`, `execute`, direct factories, aliases, fields, generic variable names, lexical local/field precedence, SQL precedence, comment/string sanitization, null timeout args, timeout-looking names, `CONNECT_TIMEOUT_MILLIS` non-null option semantics, and builder.build propagation/negative cases. The consumer uses current-file Java facts with WebClient-only matching, timeoutEvidence suppression, same-line de-dupe, and fallback preservation for absent, unusable, wrong, and non-WebClient facts. Independent adapter review found no high/medium blockers and judged the fact surface safe enough for the bounded WebClient-only consumer; independent rule review found no correctness defects. Verification passed `cargo test -p backend-doctor-analysis java -- --nocapture` with 30 passed, `cargo test -p backend-doctor-rules java_timeout -- --nocapture` with 6 passed, `cargo test -p backend-doctor-rules java_ -- --nocapture` with 31 passed, `cargo test -p backend-doctor-rules -- --nocapture` with 241 passed, `cargo test --workspace --locked`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:java-webclient-facts .`, and `docker run --rm backend-doctor:java-webclient-facts --help`. Context7 Spring/Reactor docs lookup was attempted and quota-blocked, so official Spring WebClient and Reactor Netty docs were used as fallback. | Local scanner and Docker dry-run evidence only. Public release remains blocked by missing public repo/release/registry/Homebrew/SARIF/security-owner evidence; this does not claim public release readiness, all-language HTTP-timeout parity, full route graph readiness, Tier A graph readiness, or P0 completion. |
| PHP unserialize AnalysisFacts consumer | Passed locally; not public readiness | Commit `e72a86b` (`fix: consume php unserialize facts`) records that existing `php/unserialize-user-input` now consumes PHP AnalysisFacts for high-confidence current-file request-source to `unserialize` deserialization-sink flows. The consumer requires usable ranges, PHP facts, a deserialization sink kind/callee, conservative symbol/scope relation, and either a high-confidence unsanitized `SourceToSink` edge or direct overlap; it preserves heuristic fallback for absent, empty, unrelated, low-confidence, non-PHP, or missing-range facts, avoids same-line duplicates, and does not add SQL or command-injection fact consumption. The evidence notes a Context7 quota-blocked fallback to the official PHP manual warning not to pass untrusted user input to `unserialize` and recommending safer formats such as JSON for user-supplied data: https://www.php.net/manual/en/function.unserialize.php. Post-commit verification from a clean worktree passed: `cargo test -p backend-doctor-rules php_unserialize -- --nocapture` 8 tests, `cargo test -p backend-doctor-rules php_ -- --nocapture` 8 tests, `cargo test -p backend-doctor-rules go_ -- --nocapture` 11 tests, `cargo test -p backend-doctor-rules -- --nocapture` 110 tests with doc-tests ok, `cargo test -p backend-doctor-cli --test report_schema_gate` 12 tests, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo fmt --all --check`, and `git diff --check`; initial and final `git status --short --untracked-files=all` were clean. | Local scanner evidence only. Public release evidence, Homebrew/Ruby validation, security owner inputs, all-language parity, public publish evidence, and public validator pass remain incomplete. |
| PHP command-sink AnalysisFacts consumer | Passed locally; not public readiness | Commit `a0bbbea` (`feat: consume php command analysis facts`) records that `php/shell-exec-user-input` consumes PHP AnalysisFacts for local request-source-to-command sink flows. Final commit-gate review found no actionable regressions. Post-commit verification from a clean worktree passed `git status --short --branch --untracked-files=all` with `## master`, `git rev-parse --short HEAD` as `a0bbbea`, `git show -s --format=%s HEAD` as `feat: consume php command analysis facts`, `cargo test -p backend-doctor-rules php_shell_exec -- --nocapture` 14 tests, `cargo test -p backend-doctor-rules php_ -- --nocapture` 22 tests, `cargo fmt --all --check`, `cargo test -p backend-doctor-rules -- --nocapture` 184 tests, `cargo test --workspace --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:dry-run .`, and `docker run --rm backend-doctor:dry-run --help`. The workspace test observation included backend_doctor_analysis 92 passed, backend_doctor_cache 5 passed, backend_doctor_cli 4 passed, backend-doctor-rules 184 passed, and doc-tests with 0 failed. `.github/workflows/release-dry-run.yml` documents the Docker dry-run commands. Context7 documentation research remains quota-blocked. | Local scanner and Docker dry-run evidence only. Public release remains blocked: no `origin` remote or local tags, no visible public backend-doctor GitHub repo, npm auth missing with `E401`, no Cargo registry credentials, no Docker config, no Homebrew/Ruby local environment, expected public prereq failure remains 18 blockers and 2 warnings, and SECURITY.md owner inputs/contacts remain incomplete. |
| PHP SQL AnalysisFacts consumer | Passed locally; not public readiness | Commit `8ec7fda` (`feat: consume php sql analysis facts`) records that `php/pdo-query-string-concat` consumes PhpAdapter request/SQL sink facts for local request-source-to-SQL sink flows. The evidence covers raw SQL sink names including query, exec, select, statement, unprepared, and raw variants; prepared and parameterized SQL suppression remains intact; and PHP shell-exec SQL-style exec protections remain intact. Final reviewer found no commit-blocking issues. Post-commit verification passed `cargo test -p backend-doctor-rules php_sql -- --nocapture` 8 tests, `cargo test -p backend-doctor-rules php_shell_exec -- --nocapture` 14 tests, `cargo test -p backend-doctor-rules php_ -- --nocapture` 30 tests, `cargo fmt --all --check`, `cargo test -p backend-doctor-rules -- --nocapture` 208 tests, `cargo test --workspace --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `git diff --check`, `docker build -t backend-doctor:dry-run .`, and `docker run --rm backend-doctor:dry-run --help`. Context7 remained quota-blocked; official PHP PDO prepared-statement docs were the fallback sources: https://www.php.net/manual/en/pdo.prepare.php and https://www.php.net/manual/en/pdo.prepared-statements.php. | Local scanner and Docker dry-run evidence only. Public release remains blocked by missing remote/tags/public artifacts/credentials/SARIF/Homebrew/security owner evidence; this does not claim all-language route-to-SQL parity, full route graph readiness, public release readiness, or P0 completion. |
| Rust reqwest timeout AnalysisFacts consumer | Passed locally; not public readiness | Commit `6ec90c1` (`fix: consume rust reqwest timeout facts`) records that `rust/reqwest-client-without-timeout` now consumes Rust AnalysisFacts NetworkRequest facts conservatively. The evidence covers reqwest-specific matching, chained builder timeout evidence suppression, imported reqwest shortcut support, same-line dedupe with text fallback, and preserved fallback for absent, empty, non-Rust, unusable, or unrelated facts; SQL and command fact consumption were not added. Context7 reqwest docs research was attempted and quota-blocked, so official docs.rs `reqwest::ClientBuilder` docs were used as fallback; they show `ClientBuilder::timeout` sets the total request timeout and that `ClientBuilder` can build configured clients. Independent review before commit found two issues, a chained builder false positive and imported shortcut miss; the worker fixed both, and the second independent review found no blockers. Post-commit verification at HEAD `6ec90c1` passed from clean initial and final worktrees: `cargo test -p backend-doctor-rules rust_reqwest_timeout -- --nocapture` 7 tests, `cargo test -p backend-doctor-rules rust_ -- --nocapture` 7 tests, `cargo test -p backend-doctor-rules php_ -- --nocapture` 8 tests, `cargo test -p backend-doctor-rules -- --nocapture` 117 tests with doc-tests 0/0 ok, `cargo test -p backend-doctor-cli --test report_schema_gate -- --nocapture` 12 tests, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo fmt --all --check`, and `git diff --check`. | Local scanner evidence only. Public release evidence, Homebrew/Ruby validation, security owner inputs, all-language parity, public publish evidence, and public validator pass remain incomplete. |
| Rust SQL format AnalysisFacts consumer | Passed locally; not public readiness | Commit `dd0381e` (`feat: consume rust sql format facts`) records that `rust/sql-format-string` now consumes Rust AnalysisFacts for local request-source-to-`SqlQuery` flows instead of relying only on same-line heuristics. The consumer is gated to current-file Rust facts, Rust adapter-shaped source/sink metadata, request sources, `SqlQuery` sinks, usable source/sink ranges, unsanitized `SourceToSink` edges, same-file/symbol compatibility, text evidence tying a request-derived identifier to `format!` SQL construction reaching the sink, and same-line de-dupe against heuristic fallback. Review issues were found and fixed for source identifier substring false positives, extractor names such as `Path(id)`, `Query(q)`, and `web::Path(id)`, value-specific sanitizer suppression, and imported bare `query(&sql)` support. Independent final review found no high/medium blocker: sanitizer suppression is value-specific, raw identifier formatting still reports, and sanitized propagated identifiers suppress. Context7 Rust docs research was attempted and quota-blocked; official Rust `format!` and SQLx `query!` docs were used as fallback. Post-commit verification passed `cargo test -p backend-doctor-rules rust_sql -- --nocapture` 11 tests, `cargo test -p backend-doctor-rules rust_reqwest -- --nocapture` 7 tests, `cargo test -p backend-doctor-rules rust_ -- --nocapture` 18 tests, the four targeted `report_schema_gate` JSON/SARIF golden/schema checks with 1 passed each, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked` including backend-doctor-rules 258/258 and report_schema_gate 14/14, and `git diff --check`. | Local Rust SQL rule evidence only. This records one more rule consuming AnalysisFacts with verified Rust SQL format-flow regressions; it does not claim complete Rust analysis, all AnalysisFacts integration, all-language route-to-SQL parity, public release readiness, or P0 completion. |
| Ruby SQL interpolation AnalysisFacts consumer | Passed locally; not public readiness | Commit `2604a36` (`feat: consume ruby sql facts`) records that `ruby/sql-string-interpolation` now consumes Ruby AnalysisFacts for local request-to-`SqlQuery` flows instead of relying only on text heuristics. The Tier 3 scan path now receives AnalysisFacts, and the consumer uses current-file Ruby adapter-shaped request sources, `SqlQuery` sinks, usable source/sink ranges, unsanitized `SourceToSink` flow, interpolation SQL text evidence, and same-line fallback de-dupe. Review issues were found and fixed before commit: whole-SQL sanitizer veto no longer lets a sanitized sibling interpolation hide raw interpolation; sanitized-edge global veto no longer suppresses a distinct usable unsanitized `SourceToSink` flow; same-body mixed interpolation such as `#{unsafe_id + params[:limit].to_i}` reports once while sanitized-only bodies remain quiet; and invalid facts do not suppress text fallback. Independent final review found no high/medium blocker. Context7 was attempted first and quota-blocked; official Ruby interpolation docs and Rails SQL placeholder guidance were used as fallback. Post-commit verification passed `cargo test -p backend-doctor-rules ruby_sql -- --nocapture` 8 passed, `cargo test -p backend-doctor-rules ruby -- --nocapture` 8 passed, `cargo test -p backend-doctor-rules tier3 -- --nocapture` 4 passed, the four targeted `report_schema_gate` JSON/SARIF golden/schema checks with 1 passed each, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked` including backend-doctor-rules 266 passed and 0 failed, and `git diff --check`. | Local Ruby SQL interpolation rule evidence only. This records one more rule consuming AnalysisFacts with verified Ruby SQL interpolation regressions; it does not claim complete Ruby analysis, all Tier 3 rules consuming facts, all-language route-to-SQL parity, public release readiness, or P0 completion. |
| Python Requests timeout AnalysisFacts consumer | Passed locally; not public readiness | Commit `804d89e` (`fix: consume python request timeout facts`) records that `python/requests-without-timeout` now consumes Python AnalysisFacts NetworkRequest sink facts conservatively for Requests calls. The evidence covers current-file/current-language/source/adapter/same-file/range gates, Requests-specific callee matching, same-line dedupe with text fallback, HTTPX default-timeout non-reporting, finite timeout evidence suppression, `timeout=None` treated as missing/bad timeout, preserved fallback for absent/empty/non-Python/unusable/unrelated facts, and no SQL or command fact consumption. Context7 Requests docs research was attempted and quota-blocked, so official Requests, HTTPX, and urllib docs were used as fallback; Requests docs show requests do not time out unless timeout is explicit and `timeout=None` waits forever, while HTTPX docs show HTTPX has a default timeout. Independent review initially found a `timeout=None` suppression issue; the worker fixed it, and the second independent review found no blockers. Post-commit verification at HEAD `804d89e` passed from clean initial and final worktrees: `git show --stat --oneline --no-renames HEAD` reported `804d89e fix: consume python request timeout facts` with `crates/backend-doctor-rules/src/lib.rs | 656 ++++++++++++++++++++++++++++++++-`, 655 insertions, and 1 deletion; `cargo test -p backend-doctor-rules python_requests_timeout -- --nocapture` 7 tests, `cargo test -p backend-doctor-rules python_ -- --nocapture` 24 tests, `cargo test -p backend-doctor-rules rust_reqwest_timeout -- --nocapture` 7 tests, `cargo test -p backend-doctor-rules -- --nocapture` 124 tests with doc-tests 0/0 ok, `cargo test -p backend-doctor-cli --test report_schema_gate -- --nocapture` 12 tests, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo fmt --all --check`, and `git diff --check`. | Local scanner evidence only. No public remote/release/tag/push/hosted artifact/registry evidence, GitHub release assets, npm publish, crates.io publish, Docker registry digest/provenance/SBOM, clean install/runtime proof, SARIF/code scanning proof, Homebrew/Ruby validation, SECURITY.md supported versions/artifacts, private security reporting contact/channel, or public validator pass exists. |
| C# HttpClient timeout AnalysisFacts consumer | Passed locally; not public readiness | Commit `d137055` (`fix: consume csharp httpclient timeout facts`) records that `csharp/httpclient-without-timeout` now consumes C# AnalysisFacts NetworkRequest sink facts conservatively for HttpClient use. The evidence covers current-file/current-language/source/adapter/same-file/range gates, HttpClient-specific sink context, same-line dedupe with text fallback, receiver-scoped timeout suppression, unrelated receiver `Timeout` no longer suppressing, `SetHandlerLifetime`/handler lifetime/pooled connection lifetime non-suppression, infinite timeout non-suppression, preserved fallback for absent/empty/wrong-language/wrong-file/unusable/unrelated facts, and no SQL or command fact consumption. Context7 .NET HttpClient docs research was attempted and quota-blocked, so official Microsoft Learn docs were used as fallback: `HttpClient.Timeout` is request timeout evidence, `CancellationTokenSource` can provide per-request timeout evidence, and `SetHandlerLifetime` is handler pooling lifetime rather than request-timeout evidence. Independent review initially found an unrelated HttpClient `Timeout` suppression issue; the worker fixed it, and the second independent review found no blockers. Post-commit verification at HEAD `d137055` passed from clean initial and final worktrees: `git show --stat --oneline --no-renames HEAD` reported `d137055 fix: consume csharp httpclient timeout facts` with `crates/backend-doctor-rules/src/lib.rs | 1893 +++++++++++++++++++++++++-------`, 1510 insertions, and 383 deletions; `cargo test -p backend-doctor-analysis csharp -- --nocapture` 13 tests, `cargo test -p backend-doctor-rules csharp -- --nocapture` 17 tests, both Tier 2 language fixture gates 1 test each, `cargo test -p backend-doctor-cli --test report_schema_gate csharp -- --nocapture` 1 test, Python Requests and Rust reqwest timeout regressions 7 tests each, full `backend-doctor-rules` 133 tests with doc-tests 0/0 ok, full CLI `report_schema_gate` 12 tests, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo fmt --all --check`, and `git diff --check`. | Local scanner evidence only. No public remote/release/tag/push/hosted artifact/registry evidence, GitHub release assets, npm publish, crates.io publish, Docker registry digest/provenance/SBOM, clean install/runtime proof, SARIF/code scanning proof, Homebrew/Ruby validation, SECURITY.md supported versions/artifacts, private security reporting contact/channel, or public validator pass exists. |
| Python FastAPI Tier 2 fixture coverage | Passed locally; not public readiness | Commit `adf23bd` (`test: add python fastapi fixture coverage`) records a FastAPI-shaped Python Tier 2 bad-service fixture and persisted normalized JSON/SARIF goldens. The slice adds `fixtures/python-fastapi-bad-service/app.py`, `fixtures/python-fastapi-bad-service/pyproject.toml`, `crates/backend-doctor-cli/tests/golden/reports/python-fastapi-bad-service.json`, `crates/backend-doctor-cli/tests/golden/sarif/python-fastapi-bad-service.sarif.json`, and wiring in `crates/backend-doctor-cli/tests/report_schema_gate.rs` and `crates/backend-doctor-rules/src/lib.rs`. The fixture deterministically covers four findings: requests without timeout, SQL string formatting/interpolation, subprocess `shell=True`, and unsafe `yaml.load`. Independent review found no high/medium issues; the fixture is FastAPI-shaped and deterministic, goldens are normalized, and no incidental extra findings were introduced. Verification passed six focused `report_schema_gate` commands for Tier 2/3 JSON goldens, Tier 2/3 SARIF goldens, backend-doctor JSON schema, OASIS/GitHub-shape SARIF schema, normalized Tier 2 JSON/SARIF evidence, and release-scope language-fixture inventory; two focused `backend-doctor-rules` commands for Tier 2 language-specific findings and bounded performance sanity; `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked` including `report_schema_gate` 14/14 and backend-doctor-rules 266/266; and `git diff --check`. | Local FastAPI fixture/golden/performance coverage expansion for Python Tier 2 only. This does not claim all Python coverage, all framework coverage, all language parity, public release readiness, or P0 completion. |
| Tier 2 clean JSON/SARIF goldens | Passed locally; not public readiness | Commit `256e109` (`test: add tier2 clean report goldens`) records persisted normalized JSON and SARIF goldens for `python-good-service`, `csharp-good-service`, `php-good-service`, and `rust-good-service`. The coverage verifies report schema validation, SARIF validation, Tier 2 coverage metadata, no same-language findings, no `repo/unsupported-language` findings, and no external tool execution for the clean Tier 2 fixtures. Independent reviewer found no findings. Post-commit verification for `256e109` passed `cargo test -p backend-doctor-cli --test report_schema_gate tier2_clean -- --nocapture` with 1 passed; `cargo test -p backend-doctor-cli --test report_schema_gate -- --nocapture` with 14 passed; `cargo test -p backend-doctor-rules tier2_clean_fixtures_do_not_trigger_language_specific_findings -- --nocapture` with 1 passed and 191 filtered out; `cargo fmt --all --check`; `cargo test --workspace --locked`, including `report_schema_gate` 14 passed and backend-doctor-rules 192 passed; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `git diff --check`; `docker build -t backend-doctor:dry-run .` with image `docker.io/library/backend-doctor:dry-run` and manifest list `sha256:52a6b2891a2124f0ea092917da209058db96cf50abf5ca86026d3224c6a1b1b9`; and `docker run --rm backend-doctor:dry-run --help`. | Local clean-report and golden evidence only. This does not claim public release readiness, Tier 2 production-grade language parity, Homebrew/Ruby/macOS validation, registry publishing, hosted SARIF/code-scanning proof, SECURITY.md owner completion, public validator pass, broad real-world corpus coverage, or broad benchmark evidence. |
| Suppressed findings performance/metrics baseline | Passed locally; not public readiness | Commit `ec8f0c9` (`test: add suppressed findings performance baseline`) records a rules-level deterministic synthetic suppression baseline for many findings and many suppressions. The test builds 128 placeholder findings with 96 path-scoped suppressed findings and 32 visible findings, verifies `Summary` suppressed/active counts, and enforces a generous bounded runtime. This is intentionally rules-only metric coverage: no CLI schema/golden changes, no public validators, no Homebrew/macOS/SECURITY changes, and no SQL/command AnalysisFacts changes. Independent review found no blockers and ruled rules-only Summary metric coverage sufficient for this local readiness slice. Post-commit verification at HEAD `ec8f0c9` passed from clean initial and final worktrees: `git show --stat --oneline --no-renames HEAD` reported `ec8f0c9 test: add suppressed findings performance baseline` with `crates/backend-doctor-rules/src/lib.rs | 121 insertions`; targeted suppressed baseline, `performance`, `csharp`, and `python_requests_timeout` rules tests passed; full `backend-doctor-rules` passed with 134 tests; CLI `report_schema_gate` passed with 12 tests; `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, and `git diff --check` passed. | Local metrics/performance evidence only. No public remote/release/tag/push/hosted artifact/registry evidence, GitHub release assets, npm publish, crates.io publish, Docker registry digest/provenance/SBOM, clean install/runtime proof, SARIF/code scanning proof, Homebrew/Ruby validation, SECURITY.md supported versions/artifacts, private security reporting contact/channel, or public validator pass exists. |
| Suppression broad-scope hardening | Passed locally; not public readiness | Commit `21c3409` (`fix: require explicit broad suppressions`) records that pathless or empty-path suppressions no longer silently suppress every same-rule finding unless `allowBroad`/`allow_broad` is explicitly enabled. `allowBroad` defaults false, path-scoped suppressions remain compatible, different-rule findings are not suppressed, and config/docs/schema/report schema tests were updated so the schema allows a documented omitted path with explicit broad opt-in. Independent review first found a schema/docs mismatch where the schema still required `path`; the worker fixed it by making `path` optional and updating the schema gate to omit `path`, and the second independent review found no blockers. Post-commit verification at HEAD `21c3409` passed from clean initial and final worktrees: `git show --stat --oneline --no-renames HEAD` reported `21c3409 fix: require explicit broad suppressions` with 6 files changed, 225 insertions, and 6 deletions; `cargo test -p backend-doctor-rules suppression -- --nocapture` 4 tests passed; `cargo test -p backend-doctor-core config -- --nocapture` 4 tests passed; `cargo test -p backend-doctor-cli --test report_schema_gate -- --nocapture` 13 tests passed; full `backend-doctor-rules` passed with 138 tests and doc-tests 0 passed; `cargo test --workspace --all-targets --locked`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, and `git diff --check` passed. | Local suppression-policy evidence only. No public-release validators, Homebrew/macOS paths, SECURITY owner fields, SQL/command AnalysisFacts behavior, or external evidence docs changed. No public remote/release/tag/push/hosted artifact/registry evidence, GitHub release assets, npm publish, crates.io publish, Docker registry digest/provenance/SBOM, clean install/runtime proof, SARIF/code scanning proof, Homebrew/Ruby validation, SECURITY.md supported versions/artifacts, private security reporting contact/channel, public validator pass, production readiness, P0 completion, all-language parity, public release completion, or public suppression-policy parity exists. |
| Acceptance-gates local evidence | Mapped with gaps | Local evidence exists for semantic metrics, cache replay/hit ratio, report-size ceilings, generated-artifact cache handling, false-positive guards, selected generated/vendor/dependency low-signal agent-pattern suppression, selected generated/vendor/dependency Node/TypeScript plus Go/Java semantic-duplicate suppression, generic secret env-var reference false-positive handling at `5b6ecf5`, bounded synthetic 12-service monorepo detector/scanner behavior, deterministic suppressed-finding Summary count and bounded runtime evidence at `ec8f0c9`, representative monorepo/generated-output/parser coverage, persisted report golden snapshots including the recent C#/PHP, Kotlin/Scala, C/C++, Tier 2 clean, Tier 3 clean JSON/SARIF golden commits (`0e9d1ee`, `875d740`, `47f6f1a`, `256e109`, `a18be1c`), Ruby Rails-shaped Tier 3 bad-service JSON/SARIF goldens at `32a49ba`, Python FastAPI-shaped Tier 2 bad-service JSON/SARIF goldens plus bounded performance sanity at `adf23bd`, and 10k report smoke coverage. Latest no-local-slice audit at `2026-05-14T19:09:23+02:00` predates later local evidence slices and should not be read as public-release completion. | Do not claim full acceptance-gates completeness. Broad real-world corpus coverage, hosted/public runtime evidence, public release evidence, and broad benchmark evidence remain incomplete unless the release owner promotes them into hard P0 scope. |
| Cargo/crates.io release path | Manifest-ready locally; public registry blocked | Manifest-readiness progressed with internal dependency versions, safe metadata, passing Cargo validation, and `cargo publish --dry-run --package backend-doctor-core`. Dependent dry-runs remain blocked until internal crates are published to crates.io in dependency order. Current external Cargo checks show no `backend-doctor` public result/install path; the package is currently `backend-doctor-cli`, the binary is `backend-doctor`, and the release owner must decide whether the final install command is `cargo install backend-doctor-cli` or a crate rename to `backend-doctor`. | Cargo/crates.io is a public-release evidence gate. Do not claim public readiness until crates.io package/version, release order, publish, yank/ownership, and clean `cargo install` evidence exist. |
| Latest release-gate hardening | Guarded locally; public evidence blocked | Latest 2026-05-17 broad local verification at HEAD `dd1ab4f960cc32fbb4496e9f7ed21eacec63d743` confirmed public release checks still fail closed when hosted evidence is absent. `scripts/release/check-public-release-prereqs.sh` failed as expected with 18 blockers and 2 warnings for missing origin, `BACKEND_DOCTOR_RELEASE_VERSION`, public repo/Homebrew/npm/Cargo/Docker/SARIF readiness variables, and brew/ruby tooling warnings. `.orchestration/validate-p0-release-evidence.sh` failed as expected because the evidence file still contains placeholders and weak/invalid TODO evidence for public release, workflow, npm, Docker, Cargo, Homebrew, SARIF, runtime, privacy, docs, and final gate rows. | These are release-gate safeguards, not proof of public P0 completion. |
| Coordination packet validation | Verified locally with public evidence blocked | Latest 2026-05-17 broad verification preserved clean initial/final worktrees and passing `git diff --check` while the public evidence validator remained fail-closed because hosted evidence fields are unfilled and public provenance/runtime/docs evidence is missing. | This validates local documentation hygiene only. Public release evidence remains blocked until hosted evidence is filled and validated. |
| Public release blocker verdict | Externally blocked | Expected fail-closed external blockers remain: `scripts/release/check-public-release-prereqs.sh` exits 1 with 18 blockers and 2 warnings due missing public release configuration, environment, readiness evidence, remote evidence, and brew/ruby tooling warnings; `.orchestration/validate-p0-release-evidence.sh` exits 1 because the evidence file still contains placeholders and weak/invalid TODO evidence across public release, workflow, npm, Docker, Cargo, Homebrew, SARIF, runtime, privacy, docs, and final gate rows. Homebrew/Ruby gates were not run because brew and ruby are absent. No public release, tag, push, registry publishing, hosted attestation, or SARIF evidence has been performed. | Do not claim public production readiness until hosted/public evidence exists and `.orchestration/validate-p0-release-evidence.sh` passes against filled evidence. |

## Current Known Blockers

Release owners should start with `.orchestration/p0-release-owner-unblock-request.md`, refresh blocker status from `.orchestration/p0-external-blocker-evidence.md`, and avoid public-readiness claims until `.orchestration/validate-p0-release-evidence.sh` passes against filled public evidence.

- Public release artifact URLs/remotes and publishing credentials are not configured.
- npm publish, Cargo/crates.io publish/install, Docker registry push, Homebrew tap publish, GitHub release upload, and GitHub-hosted code scanning upload were intentionally not attempted.
- SECURITY.md owner inputs remain externally blocked: exact supported-version window and real private security reporting contact/channel must be supplied by the release owner, not invented from local evidence.
- Homebrew formula syntax/audit requires macOS with Ruby/Homebrew; local generation passed, but audit was skipped because Ruby/Homebrew are unavailable in this environment.
- Network-backed external scanner execution remains opt-in and was not used for release verification.
